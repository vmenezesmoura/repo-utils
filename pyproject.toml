[project]
name = "repo-utils"
version = "0.1.0"
description = "Utilities for engineering workflows"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"